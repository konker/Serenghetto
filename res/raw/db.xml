<?xml version="1.0" encoding="utf-8"?> 
<db>
  <query type="SQL" class="*" name="create_barcodes">
    CREATE TABLE barcodes
    (
        _id INT PRIMARY KEY,
        user_id INT NOT NULL,
        code TEXT NOT NULL,
        name TEXT DEFAULT NULL,
        lat NUMBER DEFAULT NULL,
        lng NUMBER DEFAULT NULL,
        accuracy NUMBER DEFAULT NULL
    )
  </query>
  <query type="SQL" class="*" name="query_by_user">
    SELECT * FROM barcode where user_id = ?
  </query>
</db>
